{
  "name": "@cracked-judge/host",
  "version": "v0.0.1",
  "type": "module",
  "module": "./src/index.ts",
  "private": true,
  "scripts": {
    "build": "bun build --target bun ./src/index.ts",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts",
    "dev": "bun run ./src/index.ts",
    "test": "bun test ./"
  },
  "dependencies": {
    "@cracked-judge/common": "workspace:*"
  }
}
