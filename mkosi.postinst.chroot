#!/bin/sh
# --- mkosi.postinst.chroot ---

set -euo pipefail

# Set root password
echo "root:root" | chpasswd

mkdir -p /tmp
chmod 1777 /tmp



pushd /app/drivers/cpp26/include >/dev/null
./precompile.sh
popd >/dev/null

# echo "" > /etc/issue